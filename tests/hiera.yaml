---
version: 5
defaults:
        datadir: data
        data_hash: yaml_data
hierarchy:
        - name: "compliance_markup"
          lookup_key: "compliance_markup::enforcement"
        - name: "common"
          path: "common.yaml"

